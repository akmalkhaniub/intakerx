# 🤝 Contributing to IntakeRx

IntakeRx is a HIPAA-hardened clinical pre-screening platform built on React, Express, PostgreSQL (pgvector), and FastAPI.

## 🛠️ Local Setup
```bash
docker compose up -d  # Launches pgvector and backend
npm install
npm run typecheck
```
